import 'package:flutter/material.dart';

class RequestVisitPage extends StatelessWidget {
  const RequestVisitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RequestVisitPage')),
      body: const Center(child: Text('Página About')),
    );
  }
}

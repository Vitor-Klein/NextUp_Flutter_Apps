import 'package:flutter/material.dart';

class QuestionFormPage extends StatelessWidget {
  const QuestionFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('QuestionFormPage')),
      body: const Center(child: Text('Página About')),
    );
  }
}
